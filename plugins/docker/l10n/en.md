# Docker

- Manage containers: start/stop/pause/restart, restart policy, export/backup, logs
- Save and run Compose projects (YAML + optional Dockerfile)

Enable under **Settings → Plugins**.

License: **GPL-3.0** (same as ALWM).
