# Sensors (ALWM plugin)

Stats-style **Sensors** chip for the ALWM workspace bar.

- Chip: `50°` (hottest CPU die / primary)
- Click: grouped temperature list (CPU, GPU, PMU, Battery, NAND, …) with °C/°F toggle
- Optional CPU/GPU heat alerts via macOS notifications

Enable in **Settings → Plugins**.
